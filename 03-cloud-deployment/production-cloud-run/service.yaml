# ============================================================
# service.yaml — Cloud Run Service Definition (IaC)
#
# Deploy:
#   gcloud run services replace service.yaml --region=asia-southeast1
# ============================================================

apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: ai-agent
  annotations:
    run.googleapis.com/ingress: all    # public
spec:
  template:
    metadata:
      annotations:
        # ──────────────────────────────────────
        # Scaling config
        # ──────────────────────────────────────
        autoscaling.knative.dev/minScale: "0"    # scale-to-zero cho môi trường học/demo
        autoscaling.knative.dev/maxScale: "10"   # tối đa 10 instances

        # Concurrency: mỗi instance xử lý tối đa 80 requests đồng thời
        autoscaling.knative.dev/target: "80"

    spec:
      containerConcurrency: 80
      timeoutSeconds: 60

      containers:
        - image: asia-southeast1-docker.pkg.dev/PROJECT_ID/cloud-run-source-deploy/ai-agent:latest
          ports:
            - containerPort: 8000

          # ──────────────────────────────────────
          # Resources
          # ──────────────────────────────────────
          resources:
            limits:
              cpu: "1"
              memory: 512Mi
            requests:
              cpu: "0.5"
              memory: 256Mi

          # ──────────────────────────────────────
          # Environment variables
          # ──────────────────────────────────────
          env:
            - name: ENVIRONMENT
              value: production
            - name: PORT
              value: "8000"
          # ──────────────────────────────────────
          # Health checks
          # ──────────────────────────────────────
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30

          startupProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            failureThreshold: 10
            periodSeconds: 3
